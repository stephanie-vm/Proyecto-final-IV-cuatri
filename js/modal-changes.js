// imports from other modules:
import {
  openModalChanges, closeModalChanges, overlayChanges,
} from './modules/util.js';
import {
  modal,
} from './modules/events.js';

// execute functions and methods:
modal(openModalChanges, closeModalChanges, overlayChanges);
